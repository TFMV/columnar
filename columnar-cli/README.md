# columnar-cli

Optional CLI for inspecting Columnar files and running local queries. Currently a stub binary; library crates under the workspace provide the core functionality.

Build the binary with:

`cargo build -p columnar-cli`
