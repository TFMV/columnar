# columnar-format

Defines the Columnar file format: headers, schema blocks, column directory, and chunk layout as described in the workspace `spec.md`.

This crate is intentionally dependency-light; execution and I/O live in sibling crates.
